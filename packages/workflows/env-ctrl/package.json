{
  "name": "@ctrlplane/workflows--env-ctrl",
  "version": "0.0.1",
  "private": true,
  "description": "Environment controller workflow to control maximum parallel activities.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "typescript": "~4.6"
  },
  "dependencies": {
    "@kubernetes/client-node": "^0.16.3",
    "temporalio": "^0.22.0",
    "winston": "^3.7.2"
  }
}
