{
  "name": "@ctrlplane/env-ctrl-workflow",
  "version": "0.0.1",
  "private": true,
  "description": "Environment controller workflow to control maximum parallel activities.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "dev.worker": "tsc -p tsconfig.dev.json --watch"
  },
  "devDependencies": {
    "@ctrlplane/tsconfig": "*",
    "typescript": "~4.6"
  },
  "dependencies": {
    "temporalio": "^0.22.0",
    "winston": "^3.7.2"
  }
}
